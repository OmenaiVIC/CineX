;; Auto-generated Rendezvous sanity test for yield-escrow

(begin
  (print "Running generated sanity test for yield-escrow...")
  (let ((expected 1) (result (+ 1 0)))
    (asserts! (is-eq expected result) "Basic sanity check failed")
  )
  (ok true)
)
