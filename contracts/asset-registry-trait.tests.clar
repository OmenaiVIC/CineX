;; Auto-generated Rendezvous sanity test for asset-registry-trait

(begin
  (print "Running generated sanity test for asset-registry-trait...")
  (let ((expected 1) (result (+ 1 0)))
    (asserts! (is-eq expected result) "Basic sanity check failed")
  )
  (ok true)
)
